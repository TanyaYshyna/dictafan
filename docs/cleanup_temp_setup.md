# 🧹 Настройка автоматической очистки папки temp

## 📋 Зачем это нужно?

Папка `static/data/temp/` содержит временные файлы при редактировании диктантов. Если пользователь некорректно закрыл страницу редактирования, файлы могут остаться в temp и занимать место.

**Автоматическая очистка:**
- ✅ Удаляет старые временные файлы
- ✅ Освобождает место на диске
- ✅ Предотвращает накопление "мусора"

---

## 🚀 Быстрый старт

### Ручной запуск:

```bash
# Тестовый режим (посмотреть, что будет удалено)
python scripts/cleanup_temp.py --dry-run

# Реальная очистка (удалить файлы старше 3 дней)
python scripts/cleanup_temp.py

# Удалить файлы старше 7 дней
python scripts/cleanup_temp.py --days 7
```

---

## ⏰ Автоматизация

### Вариант 1: Cron Job (локально или на сервере)

#### На macOS/Linux:

1. Открой crontab:
```bash
crontab -e
```

2. Добавь строку для ежедневного запуска в 3:00 ночи:
```cron
0 3 * * * cd /Users/tanyayushyna/Documents/python_pr/dictafan && /Users/tanyayushyna/Documents/python_pr/dictafan/venv/bin/python scripts/cleanup_temp.py >> logs/cleanup.log 2>&1
```

**Важно:** Замени пути на свои!

3. Сохрани и выйди (в vim: `:wq`, в nano: `Ctrl+X`, затем `Y`)

#### Проверка cron:

```bash
# Посмотреть текущие задачи
crontab -l

# Проверить логи (если настроены)
tail -f logs/cleanup.log
```

---

### Вариант 2: Railway Scheduled Tasks

Railway поддерживает scheduled tasks через `railway.toml`:

1. Создай файл `railway.toml` в корне проекта:

```toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app:app"

# Scheduled tasks
[[tasks]]
name = "cleanup-temp"
schedule = "0 3 * * *"  # Каждый день в 3:00 UTC
command = "python scripts/cleanup_temp.py"
```

2. Railway автоматически запустит задачу по расписанию

**Примечание:** Проверь, поддерживает ли твой план Railway scheduled tasks.

---

### Вариант 3: Встроенная очистка при запуске приложения

Можно добавить очистку прямо в `app.py`, чтобы она запускалась при старте сервера:

```python
# В app.py, после импортов
from scripts.cleanup_temp import cleanup_temp_folder
import threading

def start_cleanup_scheduler():
    """Запускает очистку temp каждые 24 часа"""
    def cleanup_loop():
        while True:
            time.sleep(24 * 60 * 60)  # 24 часа
            cleanup_temp_folder(days=3, dry_run=False)
    
    thread = threading.Thread(target=cleanup_loop, daemon=True)
    thread.start()

# При запуске приложения
if __name__ == '__main__':
    # Запускаем очистку в фоне
    start_cleanup_scheduler()
    app.run(...)
```

**Плюсы:** Работает автоматически, не нужен cron  
**Минусы:** Запускается только когда приложение работает

---

### Вариант 4: GitHub Actions (если используешь GitHub)

Создай файл `.github/workflows/cleanup.yml`:

```yaml
name: Cleanup Temp Files

on:
  schedule:
    # Каждый день в 3:00 UTC
    - cron: '0 3 * * *'
  workflow_dispatch:  # Можно запустить вручную

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run cleanup
        env:
          B2_ENABLED: ${{ secrets.B2_ENABLED }}
          B2_APPLICATION_KEY_ID: ${{ secrets.B2_APPLICATION_KEY_ID }}
          B2_APPLICATION_KEY: ${{ secrets.B2_APPLICATION_KEY }}
          B2_BUCKET_NAME: ${{ secrets.B2_BUCKET_NAME }}
        run: |
          python scripts/cleanup_temp.py
```

**Примечание:** Это работает только если у тебя есть доступ к серверу через SSH или API.

---

## 🔧 Настройка параметров

### Изменить количество дней:

```bash
# Удалять файлы старше 7 дней
python scripts/cleanup_temp.py --days 7

# Удалять файлы старше 1 дня (более агрессивно)
python scripts/cleanup_temp.py --days 1
```

### Тестовый режим:

Всегда сначала запускай с `--dry-run`, чтобы посмотреть, что будет удалено:

```bash
python scripts/cleanup_temp.py --days 3 --dry-run
```

---

## 📊 Что делает скрипт?

1. **Проверяет папку** `static/data/temp/`
2. **Находит все папки** диктантов
3. **Проверяет дату создания** каждой папки
4. **Удаляет папки**, которые старше указанного количества дней
5. **Показывает статистику**: сколько удалено файлов и папок, сколько освобождено места

---

## ⚠️ Важно!

1. **Сначала тестируй** с `--dry-run`
2. **Проверь логи** после первого запуска
3. **Убедись**, что скрипт не удаляет нужные файлы
4. **Настрой бэкап** перед автоматизацией (на всякий случай)

---

## 🧪 Тестирование

### 1. Создай тестовые файлы:

```bash
# Создай старую папку (для теста)
mkdir -p static/data/temp/test_old_dictation
touch static/data/temp/test_old_dictation/test.mp3

# Измени дату создания (на macOS/Linux)
touch -t 202401010000 static/data/temp/test_old_dictation
```

### 2. Запусти тестовый режим:

```bash
python scripts/cleanup_temp.py --days 3 --dry-run
```

### 3. Проверь результат и запусти реальную очистку:

```bash
python scripts/cleanup_temp.py --days 3
```

---

## 📝 Пример вывода

```
============================================================
🧹 ОЧИСТКА ПАПКИ TEMP
============================================================
📂 Папка: /path/to/static/data/temp
⏰ Удаляем файлы старше 3 дней
🔍 Режим: РЕАЛЬНАЯ ОЧИСТКА
============================================================

📅 Удаляем файлы, созданные до: 2025-01-15 03:00:00

🗑️  Удаляем: dictation_1234567890
   📅 Создан: 2025-01-10 10:30:00 (5 дней назад)
   📁 Файлов: 15
   💾 Размер: 2340 KB
   ✅ Удалено

✅ Оставляем: dictation_9876543210 (1 дней)

============================================================
📊 ИТОГОВАЯ СТАТИСТИКА
============================================================
📁 Папок удалено: 1
📄 Файлов удалено: 15
💾 Освобождено места: 2 MB

✅ Очистка завершена!
```

---

## 🔄 Интеграция с B2

Если файлы уже мигрированы в B2, скрипт удалит только локальные копии. Файлы в B2 останутся нетронутыми.

Если хочешь также удалять из B2, можно расширить скрипт:

```python
# В cleanup_temp.py можно добавить
from helpers.b2_storage import b2_storage

# При удалении локальной папки также удалять из B2
if b2_storage.enabled:
    remote_path = f"temp/{dictation_id}/..."
    b2_storage.delete_file(remote_path)
```

---

## ❓ Частые вопросы

### 1. Безопасно ли это?

**Да**, если:
- Тестируешь с `--dry-run` сначала
- Правильно настроен параметр `--days`
- Файлы уже сохранены в финальную папку `dictations/`

### 2. Что если пользователь редактирует диктант 4 дня?

Если диктант редактируется, файлы обновляются (меняется дата модификации), поэтому они не будут удалены.

### 3. Можно ли восстановить удалённые файлы?

**Нет**, если не настроен бэкап. Поэтому важно:
- Тестировать с `--dry-run`
- Настроить бэкап
- Использовать разумное значение `--days` (3-7 дней)

### 4. Как часто запускать?

**Рекомендуется:** Раз в день (например, в 3:00 ночи)

---

## 🎯 Рекомендации

1. **Начни с 7 дней** — более безопасно
2. **Мониторь логи** первые несколько дней
3. **Уменьшай до 3 дней**, если всё работает хорошо
4. **Настрой бэкап** перед автоматизацией

