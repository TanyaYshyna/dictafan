# 📦 Руководство по миграции в Backblaze B2

## 🎯 Что мигрируется?

- ✅ **Аудиофайлы** из `static/data/dictations/` и `static/data/temp/`
- ✅ **Обложки диктантов** (`cover.webp`)
- ✅ **Аватары пользователей** (`avatar.webp`, `avatar_min.webp`)

**НЕ мигрируется:**
- ❌ JSON файлы (остаются локально, потом перейдём на БД)
- ❌ Данные пользователей (потом перейдём на БД)

---

## 🚀 Быстрый старт

### 1. Подготовка

Убедись, что B2 настроен:

```bash
# Проверь переменные окружения в .env
B2_ENABLED=true
B2_APPLICATION_KEY_ID=твой_keyID
B2_APPLICATION_KEY=твой_applicationKey
B2_BUCKET_NAME=dictafan-dictations
```

### 2. Тестовый запуск (dry-run)

Посмотри, что будет мигрировано (без загрузки):

```bash
python scripts/migrate_to_b2.py --dry-run
```

### 3. Реальная миграция

```bash
# Всё сразу
python scripts/migrate_to_b2.py

# Или по частям
python scripts/migrate_to_b2.py --dictations-only  # Только диктанты
python scripts/migrate_to_b2.py --avatars-only      # Только аватары
```

---

## 📊 Что покажет скрипт?

```
🚀 МИГРАЦИЯ В BACKBLAZE B2
============================================================
Bucket: dictafan-dictations
Режим: РЕАЛЬНАЯ МИГРАЦИЯ
============================================================

📁 МИГРАЦИЯ ДИКТАНТОВ (аудио + обложки)
============================================================
📂 Обработка: static/data/dictations
  📝 Диктант: dicta_001
    ✅ Обложка: dictations/dicta_001/cover.webp (45 KB)
    ✅ Аудио: dictations/dicta_001/en/001.mp3 (234 KB)
    ✅ Аудио: dictations/dicta_001/en/002.mp3 (189 KB)
    ...

📊 ИТОГОВАЯ СТАТИСТИКА
============================================================
📁 Диктанты:
   Аудиофайлов: 150
   Обложек: 12
   Размер: 45 MB

👤 Аватары:
   Файлов: 8
   Размер: 320 KB

📦 Всего:
   Файлов: 170
   Общий размер: 45 MB

✅ Миграция завершена!
```

---

## 🔄 Что происходит после миграции?

### Существующие файлы:

- ✅ Загружены в B2
- ✅ Остаются локально (можно удалить позже)
- ✅ Новые файлы автоматически загружаются в B2

### Новые файлы:

- ✅ Автоматически загружаются в B2 при создании
- ✅ Сохраняются локально для обработки
- ✅ URL из B2 используется в коде

---

## ❓ Частые вопросы

### 1. Можно ли запустить миграцию несколько раз?

**Да!** Скрипт загрузит файлы заново (может быть дублирование). B2 SDK автоматически обрабатывает это.

### 2. Что делать, если миграция прервалась?

**Запусти снова!** Скрипт продолжит с того места, где остановился. Уже загруженные файлы будут перезаписаны.

### 3. Можно ли удалить локальные файлы после миграции?

**Да, но осторожно!** 
- Убедись, что всё загружено в B2
- Проверь, что приложение работает
- Сделай бэкап перед удалением

### 4. Как проверить, что файлы в B2?

1. Зайди в B2 Dashboard
2. Выбери свой bucket
3. Нажми "Browse Files"
4. Проверь структуру папок

### 5. Что делать с ошибками?

Если скрипт показывает ошибки:
1. Проверь переменные окружения
2. Проверь права Application Key
3. Проверь интернет-соединение
4. Запусти снова (ошибки могут быть временными)

---

## 🎯 Следующие шаги

1. ✅ Мигрировать аудио и аватары (этот скрипт)
2. ⏳ Добавить базу данных
3. ⏳ Мигрировать JSON файлы в БД
4. ⏳ Обновить код для чтения из БД
5. ⏳ Удалить локальные файлы (опционально)

---

## 📝 Примеры использования

### Миграция только новых файлов:

Если добавил новые диктанты, можно мигрировать только их:

```bash
# Скрипт автоматически найдёт все файлы
python scripts/migrate_to_b2.py --dictations-only
```

### Проверка перед миграцией:

```bash
# Сначала тестовый режим
python scripts/migrate_to_b2.py --dry-run

# Потом реальная миграция
python scripts/migrate_to_b2.py
```

### Миграция только аватаров:

```bash
python scripts/migrate_to_b2.py --avatars-only
```

---

## ⚠️ Важно!

1. **Делай бэкап** перед миграцией (на всякий случай)
2. **Тестируй** в dry-run режиме сначала
3. **Проверяй** результаты в B2 Dashboard
4. **Не удаляй** локальные файлы сразу после миграции

